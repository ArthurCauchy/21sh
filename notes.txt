TODO :
- heredoc (<<)
- cat /dev/random | ls - LITTLE LESS BROKEN
- ctrl+D
- Whenever the child process wants to read from the pipe, it must first close the pipe's side from writing.
- FIX waitpid dans le cas des pipes pas forcement clean (waitpid() error may happen)
- FIX ctrl-z on env builtin (env ls -lR /)
- interdire certains builtins dans un pipe / builtin env
- FIX "ls >&- <&-"
- FIX pipe exit code (ls qweeqwe | cat || echo def)
- CDPATH
- FIX last exit code in lexing_dollar.c

TODO bonus :
- 21shrc
- builtin help

Notes :
- pour voir les file descriptors ouverts : lsof -p <pid>

csh :
- 'exit qweqwejiowqe' : Expression syntax.
- '""' : : Command not found.
- 'ls >' : Missing name for redirect.
- 'ls > &&' : Missing name for redirect.
- 'env ls > test' : print in test
- 'echo ~' avec env vide : 'No $HOME variable set'.
- 'echo abc'd'' : abcd

http://pubs.opengroup.org/onlinepubs/009695399/utilities/xcu_chap02.html#tag_02_07
