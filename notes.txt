TODO :
- cat /dev/random | ls - COMPLETELY BROKEN
- heredoc (<<)
- Command not found + redirection : actuellement comme sh (OK), a modifier pour faire comme csh (mieux).
- FIX : ls >&3 : should be wrong but OK
- FIX fichiers ouverts jamais fermes
- tilde and var expansion
- ctrl+D
- faire les redirections bien
- FIX : stopper toute la chaine de commande a la reception d'un ctrl C ou ctrl Z
- Whenever the child process wants to read from the pipe, it must first close the pipe's side from writing.
- FIX : return of pipe
- FIX REDIRECTIONS FIRST

TODO bonus :
- refaire cd : CDPATH
- refaire pwd (ou le suppr ?- voir sujet 42sh)
- 21shrc
- builtin help
- tilde expansion
- pourquoi est-ce que c'est pas builtin_parse_options qui fait le bzero ? :o
- tester CDPATH (ex: fichier ou mauvais mode et un un vrai dossier dans un CDPATH plus loin)

Notes :
- pour voir les file descriptors ouverts : lsof -p <pid>
- Pipe non fonctionnels (ne demarrent pas en meme temps). A fix, faire le job control en même temps.
- Un builtin dans un pipe est fork !

csh :
- 'exit qweqwejiowqe' : Expression syntax.
- '""' : : Command not found.
- 'ls >' : Missing name for redirect.
- 'ls > &&' : Missing name for redirect.


http://pubs.opengroup.org/onlinepubs/009695399/utilities/xcu_chap02.html#tag_02_07
